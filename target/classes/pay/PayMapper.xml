<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pay.PayMapper">
	<select id="getNextPaymentId" resultType="int">
	    SELECT payment_id_seq.NEXTVAL FROM dual
	</select>


<!-- 결제 및 주문상품을 각각 행으로 저장 -->
  <insert id="insertPayments" parameterType="map">
    <foreach collection="items" item="item" separator=";">
      INSERT INTO payments (
        payment_item_id, payment_id, u_id, name, num, address, post, detail_address, title, quantity, price
      )
      VALUES (
        payment_item_seq.NEXTVAL, #{paymentId}, #{userId}, #{name}, #{num}, #{address}, #{post}, #{detailAddress}, 
        #{item.title}, #{item.quantity}, #{item.price}
      )
    </foreach>
  </insert>
</mapper>
